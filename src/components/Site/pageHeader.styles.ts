import palette from 'src/theme/palette.theme'

const styles = {
  container: {
    padding: '2rem 0',
    width: '100%',
    marginTop: '2rem',
  },
  header: {
    color: palette.background.contrast,
  },
} as const

export default styles
