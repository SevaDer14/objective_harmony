import { Typography } from '@mui/material'

const HomePage = () => {
  return (
    <Typography variant="h1" sx={{ textAlign: 'center' }}>
      Hello World
    </Typography>
  )
}

export default HomePage
